using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.Events;

public class EventOnMicInput : MonoBehaviour
{
    /*[SerializeField] private float minimumTriggerVolume = .1f;
    [SerializeField] private float waitAfterTrigger = 1f;
    private float t;
    
    [SerializeField] private UnityEvent volumeEvent;
    
    private void Update()
    {
        t += Time.deltaTime;
        
        if (FakeVoiceRecognition.MicLoudness > minimumTriggerVolume && t >= waitAfterTrigger)
        {
            volumeEvent.Invoke();
            t = 0f;
        }
    }*/
}
